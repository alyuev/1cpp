// сигнатура для теста ПолучитьТекстТекущегоМодуля - НЕ ИЗМЕНЯТЬ

Функция Конт(Конт) Возврат Конт; КонецФункции // : Контекст
Функция Сам() Возврат Конт(Контекст); КонецФункции

Функция Информатор()
	//Возврат СоздатьОбъект("Информатор");
	Информатор1 = СоздатьОбъект("Информатор");
	Возврат Информатор1;
КонецФункции

Процедура ТестСоздатьОбъект() Экспорт
	Объект = Информатор();
КонецПроцедуры

Процедура ТестПолучитьТаблицуМетодовДляСтроки() Экспорт
	Сам = Сам();

	Информатор = Информатор();                    
	табМетодов = Информатор.ПолучитьТаблицуМетодов("Строка");

	Сам.ПроверитьТипЗначения(табМетодов, "ТаблицаЗначений");
	Сам.ПроверитьРавенство(табМетодов.КоличествоСтрок(), 0);

КонецПроцедуры

Процедура ТестПолучитьТаблицуСвойствДляСтроки() Экспорт
	Сам = Сам();

	Информатор = Информатор();                    
	ТабСвойств = Информатор.ПолучитьТаблицуСвойств("Строка");	

	Сам.ПроверитьТипЗначения(ТабСвойств, "ТаблицаЗначений");
	Сам.ПроверитьРавенство(ТабСвойств.КоличествоСтрок() , 0);

КонецПроцедуры

Процедура ТестПолучитьТаблицуМетодовДляТаблицыЗначений() Экспорт
	Сам = Сам();

	Таб = СоздатьОбъект("ТаблицаЗначений");

	Информатор = Информатор();                    
	табМетодов = Информатор.ПолучитьТаблицуМетодов(Таб);
//табМетодов.ВыбратьСтроку("");
	
	Сам.ПроверитьТипЗначения(табМетодов, "ТаблицаЗначений");
	Сам.ПроверитьРавенство(табМетодов.КоличествоСтрок(), 30);
	
	табМетодов.ПолучитьСтрокуПоНомеру(1);
	Сам.ПроверитьРавенство(табМетодов.Number, 0);
	Сам.ПроверитьНеРавенство(табМетодов.NameEng, "");
	Сам.ПроверитьНеРавенство(табМетодов.NameRus, "");
	Сам.ПроверитьНеРавенство(табМетодов.HasRetVal, -1);
	Сам.ПроверитьНеРавенство(табМетодов.NParams, -1);
КонецПроцедуры

Процедура ТестПолучитьТаблицуСвойствДляТаблицыЗначений() Экспорт
	Сам = Сам();

	Таб = СоздатьОбъект("ТаблицаЗначений");

	Информатор = Информатор();                    
	ТабСвойств = Информатор.ПолучитьТаблицуСвойств(Таб);	
//ТабСвойств.ВыбратьСтроку("");

	Сам.ПроверитьТипЗначения(ТабСвойств, "ТаблицаЗначений");
	Сам.ПроверитьРавенство(ТабСвойств.КоличествоСтрок() , 1);
	
	ТабСвойств.ПолучитьСтрокуПоНомеру(1);
	Сам.ПроверитьРавенство(ТабСвойств.Number, 0);
	Сам.ПроверитьНеРавенство(ТабСвойств.NameEng, "");
	Сам.ПроверитьНеРавенство(ТабСвойств.NameRus, "");
	Сам.ПроверитьРавенство(ТабСвойств.IsReadable, 1);
	Сам.ПроверитьРавенство(ТабСвойств.IsWritable, 1);

КонецПроцедуры

Процедура ТестМетодСуществует() Экспорт
	Сам = Сам();

	Таб = СоздатьОбъект("ТаблицаЗначений");

	Информатор = Информатор();                    

	Сам.ПроверитьРавенство(Информатор.МетодСуществует(Таб, "КоличествоСтрок"), 1);
	Сам.ПроверитьРавенство(Информатор.МетодСуществует(Таб, "НесуществующийМетод"), 0);

КонецПроцедуры


Процедура ТестМетодСуществует_НеверныеПараметры() Экспорт
	Сам = Сам();

	Таб = СоздатьОбъект("ТаблицаЗначений");

	Информатор = Информатор();                    
                                                                  
	Сам.ПроверитьИсключение(Информатор, "МетодСуществует", "НеКонтекст", 1); // 1 парам не контекст
	Сам.ПроверитьИсключение(Информатор, "МетодСуществует", Таб, 1); // 2 парам не строка

КонецПроцедуры

Процедура ТестСвойствоСуществует() Экспорт
	Сам = Сам();

	Таб = СоздатьОбъект("ТаблицаЗначений");

	Информатор = Информатор();                    

	Сам.ПроверитьРавенство(Информатор.СвойствоСуществует(Таб, "НомерСтроки"), 1);
	Сам.ПроверитьРавенство(Информатор.СвойствоСуществует(Таб, "НесуществующееСвойство"), 0);

КонецПроцедуры

Процедура ТестСвойствоСуществует_НеверныеПараметры() Экспорт
	Сам = Сам();

	Таб = СоздатьОбъект("ТаблицаЗначений");

	Информатор = Информатор();                    
                                                                  
	Сам.ПроверитьИсключение(Информатор, "СвойствоСуществует", "НеКонтекст", 1); // 1 парам не контекст
	Сам.ПроверитьИсключение(Информатор, "СвойствоСуществует", Таб, 1); // 2 парам не строка

КонецПроцедуры

Процедура ТестЯвляетсяОбъектом() Экспорт
	Сам = Сам();

	Таб = СоздатьОбъект("ТаблицаЗначений");

	Информатор = Информатор();                    
                                                                  
	Сам.ПроверитьРавенство(Информатор.ЯвляетсяОбъектом(Таб, "ТаблицаЗначений"), 1);
	Сам.ПроверитьРавенство(Информатор.ЯвляетсяОбъектом(Таб, "тАБЛИЦАзНАЧЕНИЙ"), 1);
	Сам.ПроверитьРавенство(Информатор.ЯвляетсяОбъектом("Строка", "Строка"), 1);

	Сам.ПроверитьРавенство(Информатор.ЯвляетсяОбъектом("ТаблицаЗначений", "ТаблицаЗначений"), 1);

КонецПроцедуры

Процедура ТестЯвляетсяОбъектом_НеверныеПараметры() Экспорт
	Сам = Сам();

	Таб = СоздатьОбъект("ТаблицаЗначений");
	Информатор = Информатор();                    
                                                                  
	Сам.ПроверитьРавенство(Информатор.ЯвляетсяОбъектом(Таб, "НеверныйТип"), 0);
	Сам.ПроверитьРавенство(Информатор.ЯвляетсяОбъектом("", "НеверныйТип"), 0);
	Сам.ПроверитьРавенство(Информатор.ЯвляетсяОбъектом(Таб, "СписокЗначений"), 0);

	Сам.ПроверитьРавенство(Информатор.ЯвляетсяОбъектом(1, ""), 0);
                                                           
	Сам.ПроверитьИсключение(Информатор, "ЯвляетсяОбъектом", 1, "СписокЗначений"); // 1 парам не строка и не объект

КонецПроцедуры

Процедура ТестСамЯвляетсяОбъектом() Экспорт
  Сам = Сам();
  Информатор = Информатор();
  
  Сам.ПроверитьРавенство(Информатор.ЯвляетсяОбъектом(Сам, ""),0);

  Сам.ПроверитьРавенство(Информатор.ЯвляетсяОбъектом(Сам, "ИнформаторТесты"),1);
  Сам.ПроверитьРавенство(Информатор.ЯвляетсяОбъектом(Сам, "ТестовыйСлучайСДанными"),1);
  Сам.ПроверитьРавенство(Информатор.ЯвляетсяОбъектом(Сам, "ТестовыйСлучай"),1);

  Сам.ПроверитьРавенство(Информатор.ЯвляетсяОбъектом(Сам, "ВыполняемыйМодуль"),1);

  Сам.ПроверитьРавенство(Информатор.ЯвляетсяОбъектом(Сам, "ТестовыйСлучай1"),0);
КонецПроцедуры


Процедура ТестТестовыйСлучайФормыЯвляетсяОбъектом() Экспорт
  Информатор = Информатор();
  Объект = СоздатьОбъект("ТестовыйСлучайФормы"); // множественное наследование

  Сам().ПроверитьРавенство(Информатор.ЯвляетсяОбъектом(Объект, "ТестовыйСлучай"),1);
  
  Сам().ПроверитьРавенство(Информатор.ЯвляетсяОбъектом(Объект, "ТестовыйСлучайСДанными"),1);

  Сам().ПроверитьРавенство(Информатор.ЯвляетсяОбъектом(Объект, "ДелегатФормы"),1);

  Сам().ПроверитьРавенство(Информатор.ЯвляетсяОбъектом(Объект, "ВыполняемыйМодуль"),1);

  Сам().ПроверитьРавенство(Информатор.ЯвляетсяОбъектом(Объект, "ТестовыйСлучай1"),0);
КонецПроцедуры


Процедура ТестЯвляетсяФункцией() Экспорт
	Сам = Сам();

	Таб = СоздатьОбъект("ТаблицаЗначений");

	Информатор = Информатор();                    
                                                                  
	Сам.ПроверитьРавенство(Информатор.ЯвляетсяФункцией(Таб, "КоличествоКолонок"), 1);
	Сам.ПроверитьРавенство(Информатор.ЯвляетсяФункцией(Таб, "кОЛИЧЕСТВОкОЛОНОК"), 1);

	Сам.ПроверитьРавенство(Информатор.ЯвляетсяФункцией(Таб, "УдалитьСтроку"), 0);
	Сам.ПроверитьРавенство(Информатор.ЯвляетсяФункцией(Таб, "уДАЛИТЬсТРОКУ"), 0);

КонецПроцедуры

Процедура ТестЯвляетсяФункцией_ДляКОП() Экспорт
	Сам = Сам();

	Объект = СоздатьОбъект("ООППредок");

	Информатор = Информатор();                    
                                                                  
	Сам.ПроверитьРавенство(Информатор.ЯвляетсяФункцией(Объект, "ОткрытаяФункция"), 1);
	Сам.ПроверитьРавенство(Информатор.ЯвляетсяФункцией(Объект, "оТКРЫТАЯфУНКЦИЯ"), 1);

	Сам.ПроверитьРавенство(Информатор.ЯвляетсяФункцией(Объект, "ОткрытаяПроцедура"), 0);
	Сам.ПроверитьРавенство(Информатор.ЯвляетсяФункцией(Объект, "оТКРЫТАЯпРОЦЕДУРА"), 0);

КонецПроцедуры

Процедура ТестЯвляетсяФункцией_НеверныеПараметры() Экспорт
	Сам = Сам();

	Таб = СоздатьОбъект("ТаблицаЗначений");
	Информатор = Информатор();                    
                                                                  
	Сам.ПроверитьРавенство(Информатор.ЯвляетсяОбъектом(Таб, "НеверныйМетод"), 0);
	Сам.ПроверитьРавенство(Информатор.ЯвляетсяОбъектом("", "НеверныйТип"), 0);
                                                           
	Сам.ПроверитьИсключение(Информатор, "ЯвляетсяОбъектом", 1, "СписокЗначений"); // 1 парам не строка и не объект

КонецПроцедуры

Процедура ТестКоманднаяСтрока() Экспорт
// ENTERPRISE /M /D"G:\1C++\Testing 1C++\" /N /U"G:\1C++\Testing 1C++\" /T"g:\" /Lrus  /Обработка:ЗагрузчикЮнитТестов /Команда:Тестировать /Close 

	Сам = Сам();
	Информатор = Информатор();                    
	КоманднаяСтрока = Информатор.КоманднаяСтрока();
	
	Сам().ПроверитьНеравенство(КоманднаяСтрока, "");
КонецПроцедуры

Процедура ТестКоманднаяСтрока1() Экспорт

	Сам = Сам();
	ОбАнализаКоманднойСтроки = СоздатьОбъект("Общие.АнализКоманднойСтроки1С");
	Параметр = ОбАнализаКоманднойСтроки.ПолучитьПараметр( "/D" );
	
	Сам().ПроверитьНеравенство(Параметр, "");
КонецПроцедуры

Процедура ТестКоманднаяСтрокаКоличествоПараметров() Экспорт

	Сам = Сам();
	ОбАнализаКоманднойСтроки = СоздатьОбъект("Общие.АнализКоманднойСтроки1С");
	КолПараметров = ОбАнализаКоманднойСтроки.ПолучитьКоличествоПараметров();
	Сам.ПроверитьБольшеИлиРавно(КолПараметров, 1);
КонецПроцедуры

Процедура ТестПутьПриложения1С() Экспорт
// C:\Program Files\1Cv77\Bin25\1CV7.exe

	Сам = Сам();
	Информатор = Информатор();                    
	ПутьПриложения1С = Информатор.ПутьПриложения1С();
	
	Сам.ПроверитьНеравенство(ПутьПриложения1С, "");
	
	Сам.ПроверитьБольше(Найти(Врег(ПутьПриложения1С), ".EXE"), 0);
	Сам.ПроверитьБольше(Найти(Врег(ПутьПриложения1С), "1CV"), 0);
КонецПроцедуры

Процедура ТестТипЗначенияСтрВнутр() Экспорт
	Сам = Сам();
	Информатор = Информатор();                    

	Объект = СоздатьОбъект("Документ");
	ТипОбъекта = Информатор.ТипЗначенияСтрВнутр(Объект);
	Сам.ПроверитьРавенство(ТипОбъекта, "CDocContext");

	Объект = СоздатьОбъект("Документ.Новый1");
	ТипОбъекта = Информатор.ТипЗначенияСтрВнутр(Объект);
	Сам.ПроверитьРавенство(ТипОбъекта, "CDocContext");

	Объект = СоздатьОбъект("Справочник");
	ТипОбъекта = Информатор.ТипЗначенияСтрВнутр(Объект);
	Сам.ПроверитьРавенство(ТипОбъекта, "CSubcContext");

	Объект = СоздатьОбъект("Справочник.Новый2");
	ТипОбъекта = Информатор.ТипЗначенияСтрВнутр(Объект);
	Сам.ПроверитьРавенство(ТипОбъекта, "CSubcContext");

	//Объект = СоздатьОбъект("Регистр.Новый1");
	//ТипОбъекта = Информатор.ТипЗначенияСтрВнутр(Объект);
	//Сам.ПроверитьРавенство(ТипОбъекта, "CRegActContext");

	Объект = СоздатьОбъект("Регистры");
	ТипОбъекта = ТипЗначенияСтр(Объект);
	Сам.ПроверитьРавенство(ТипОбъекта, "НеизвестныйОбъект"); // невозможно получить

	ТипОбъекта = Информатор.ТипЗначенияСтрВнутр(Объект);
	Сам.ПроверитьРавенство(ТипОбъекта, "CAllRegsContext");
КонецПроцедуры

Процедура ТестТипЗначенияСтрВнутрНеверныеПараметры() Экспорт
	Сам = Сам();
	Информатор = Информатор();                    

	Объект = 1;
	Сам.ПроверитьИсключение(Информатор, "ТипЗначенияСтрВнутр", Объект); //ТипОбъекта = Информатор.ТипЗначенияСтрВнутр(Объект);

	Объект = "1";
	Сам.ПроверитьИсключение(Информатор, "ТипЗначенияСтрВнутр", Объект); //ТипОбъекта = Информатор.ТипЗначенияСтрВнутр(Объект);
КонецПроцедуры

Процедура Тест_ПолучитьВремяПростояСистемы() Экспорт
	Сам = Сам();
	Информатор = Информатор();
	
	Сам.ПроверитьТипЗначения(Информатор.ПолучитьВремяПростояСистемы(), "Число");
	ДесятьМинут_ИлиМаксимальноВозможноеВремяТестирования = 10*60*1000;
	Сам.ПроверитьМеньше(Информатор.ПолучитьВремяПростояСистемы(), ДесятьМинут_ИлиМаксимальноВозможноеВремяТестирования);
КонецПроцедуры

Процедура Тест_ПолучитьТаблицуЗагруженныхВК() Экспорт
	Сам = Сам();

	Информатор = Информатор();                    
	табЗагруженныхВК = Информатор.ПолучитьТаблицуЗагруженныхВК();
//табЗагруженныхВК.ВыбратьСтроку("");
	
	Сам.ПроверитьТипЗначения(табЗагруженныхВК, "ТаблицаЗначений");
	Сам.ПроверитьБольше(табЗагруженныхВК.КоличествоСтрок(), 0);
	
	табЗагруженныхВК.ПолучитьСтрокуПоНомеру(1);

	Сам.ПроверитьРавенство(табЗагруженныхВК.Number, 0);
	Сам.ПроверитьРавенство(Врег(табЗагруженныхВК.ИмяФайла), Врег("1cpp.dll"));
	Сам.ПроверитьНеРавенство(табЗагруженныхВК.ПутьФайла, "");
	Сам.ПроверитьРавенство(табЗагруженныхВК.ИмяОбъекта, "AddIn.1C++");
	Сам.ПроверитьНеРавенство(табЗагруженныхВК.ПутьРеестра, "");
КонецПроцедуры

Процедура Тест_ПолучитьТекстТекущегоМодуля() Экспорт
	Сам = Сам();

	Информатор = Информатор();                    
	ТекстТекущегоМодуля = Информатор.ПолучитьТекстТекущегоМодуля();
	
	Сам.ПроверитьТипЗначения(ТекстТекущегоМодуля, "Строка");
	Сам.ПроверитьРавенство(Найти(ТекстТекущегоМодуля, "// сигнатура для теста ПолучитьТекстТекущегоМодуля - НЕ ИЗМЕНЯТЬ"), 1);
КонецПроцедуры

Процедура Тест_ПолучитьТекстМодуляДляКласса() Экспорт
	Сам = Сам();

	Информатор = Информатор();                    
	ТекстМодуля = Информатор.ПолучитьТекстМодуля(Сам);
	
	Сам.ПроверитьТипЗначения(ТекстМодуля, "Строка");
	Сам.ПроверитьРавенство(Найти(ТекстМодуля, "// сигнатура для теста ПолучитьТекстТекущегоМодуля - НЕ ИЗМЕНЯТЬ"), 1);
КонецПроцедуры

Процедура Тест_ПолучитьТекстМодуляДляФормы() Экспорт
	Сам = Сам();

	Информатор = Информатор();                 
	ДелегатФормы = СоздатьОБъект("ДелегатФормы");
	КонтФормы = ДелегатФормы.ОткрытьФорму("Отчет",, КаталогИБ()+"ExtForms\ОтчетНовый1.ert");
	
	ТекстМодуля = Информатор.ПолучитьТекстМодуля(КонтФормы);
	
	Сам.ПроверитьТипЗначения(ТекстМодуля, "Строка");
	Сам.ПроверитьБольшеИлиРавно(Найти(ТекстМодуля, "Перем АтрибутМодуля;"), 1);
КонецПроцедуры

Процедура Тест_ПолучитьТекстМодуляСОшибкой() Экспорт
	Сам = Сам();

	Информатор = Информатор();                    
	Сам.ПроверитьИсключение(Информатор, "ПолучитьТекстМодуля", 1);

	ДелегатФормы = СоздатьОБъект("ДелегатФормы");
	КонтФормы = ДелегатФормы.ОткрытьФорму("Отчет",, КаталогИБ()+"ExtForms\ОтчетНовый1.ert");
	Сам.ПроверитьИсключение(Информатор, "ПолучитьТекстМодуля", КонтФормы.Форма);
КонецПроцедуры
