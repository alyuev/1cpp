Перем Журнал;

Функция Конт(Конт) Возврат Конт; КонецФункции
Функция Сам() Возврат Конт(Контекст); КонецФункции
Функция База() Возврат Сам().ПолучитьБазовыйКласс(); КонецФункции

Процедура Инит(Журнал1) Экспорт
	Журнал = Журнал1;	
КонецПроцедуры

Процедура КонструкторУстановитьАтрибут(Конт, ИДОбъектаАктивИкс, Журнал1)
	Журнал = Журнал1;	

	Сам().УстановитьАтрибут(Конт.Форма, ИДОбъектаАктивИкс);
КонецПроцедуры

Процедура ФормаПриСоздании(Источник) Экспорт
	Предупреждение("КОП::ФормаПриСоздании");
	
	Журнал.ДобавитьЗапись("КОП::ФормаПриСоздании");
	Журнал.ДобавитьЗапись("Источник="+ТипЗначенияСтр(Источник));
КонецПроцедуры

Процедура ИДОбъектаАктивИкс_BeforeNavigate2(объект,урл,флаги,фрейм,пост,заголовки,отмена) // Экспорт

	Журнал.ДобавитьЗапись("КОП::Событие::ИДОбъектаАктивИкс_BeforeNavigate2");
	Журнал.ДобавитьЗапись("URL="+урл);

	отмена=-1; // не выводить в форму - так быстрее
КонецПроцедуры
                                                   
// специально мало параметров !!
Процедура ИДДругой_BeforeNavigate2(объект) // Экспорт
	Журнал.ДобавитьЗапись("КОП::Событие::ИДДругой_BeforeNavigate2");
КонецПроцедуры

Процедура BeforeNavigate2(объект,урл,флаги,фрейм,пост,заголовки,отмена) // Экспорт

	Журнал.ДобавитьЗапись("КОП::Событие::BeforeNavigate2");
	Журнал.ДобавитьЗапись("URL="+урл);

	отмена=-1; // не выводить в форму - так быстрее
КонецПроцедуры
                                                   
//// специально мало параметров !!
//Процедура BeforeNavigate2(объект) // Экспорт
//	Журнал.ДобавитьЗапись("КОП::Событие::BeforeNavigate2");
//КонецПроцедуры
