Перем стрЖурнал Экспорт;

// -------------------------------- -------------------------------------
Процедура Конструктор()                                   
	стрЖурнал = "";	
КонецПроцедуры // Конструктор

Процедура ДобавитьЗапись(ИмяМетода) Экспорт
	Если ПустоеЗначение(стрЖурнал)=1 Тогда
		стрЖурнал=ИмяМетода;
	Иначе
		стрЖурнал=стрЖурнал+", "+ИмяМетода;
	КонецЕсли;
КонецПроцедуры
