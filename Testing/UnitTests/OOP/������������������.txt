// работа с наследованием
// ДокументПотомок

//# ClassBegin ДокументПотомок;

Перем АтрибутКласса Экспорт;

Функция Конт(Конт) Возврат Конт; КонецФункции
Функция Сам() Возврат Конт(Контекст); КонецФункции

Процедура Конструктор()

	АтрибутКласса = 2;
КонецПроцедуры	// Конструктор

Функция Функция1() Экспорт
	Возврат 2;
КонецФункции

Процедура Процедура1(Параметр1) Экспорт
КонецПроцедуры
       

//# ClassEnd ДокументПотомок;

//# ClassBegin ДокументПотомокКонкретныйДокумент;

     //оДок = СоздатьОбъект("ТДокумент.ПриходнаяНакладная");
     //Док = СоздатьОбъект("Документ.ПриходнаяНакладная");
     //оДок.НазначитьКонтекст(Док);

Функция Конт(Конт) Возврат Конт; КонецФункции
Функция Сам() Возврат Конт(Контекст); КонецФункции

//НазначитьКонтекст() - он в базовом классе, вот такой вот:
Функция НазначитьКонтекст(Конт) Экспорт
	Возврат Сам().ЗаменитьБазовыйОбъект("Документ", Конт);
КонецФункции

//# ClassEnd ДокументПотомокКонкретныйДокумент;

//# ClassBegin ДокументПотомокКонкретныйДокумент1;

     //оДок = СоздатьОбъект("ТДокумент.ПриходнаяНакладная");
     //Док = СоздатьОбъект("Документ.ПриходнаяНакладная");
     //оДок.НазначитьКонтекст(Док);

Функция Конт(Конт) Возврат Конт; КонецФункции
Функция Сам() Возврат Конт(Контекст); КонецФункции

//НазначитьКонтекст() - он в базовом классе, вот такой вот:
Функция НазначитьКонтекст(Конт) Экспорт
	//Возврат Сам().ЗаменитьБазовыйОбъект("Документ", Конт);
	Возврат Конт(Контекст).ЗаменитьБазовыйОбъект("Документ", Конт);
КонецФункции

//# ClassEnd ДокументПотомокКонкретныйДокумент1;

//# ClassBegin ДокументПотомокКонкретныйДокумент2;

     //оДок = СоздатьОбъект("ТДокумент.ПриходнаяНакладная");
     //Док = СоздатьОбъект("Документ.ПриходнаяНакладная");
     //оДок.НазначитьКонтекст(Док);

Функция Конт(Конт) Возврат Конт; КонецФункции
Функция Сам() Возврат Конт(Контекст); КонецФункции

//НазначитьКонтекст() - он в базовом классе, вот такой вот:
Функция НазначитьКонтекст(Конт) Экспорт
	Возврат Сам().ЗаменитьБазовыйОбъект("Документ", Конт);
КонецФункции

//# ClassEnd ДокументПотомокКонкретныйДокумент2;
