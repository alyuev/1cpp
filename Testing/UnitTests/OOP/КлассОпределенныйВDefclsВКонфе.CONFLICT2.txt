// работа с динамическими свойствами

//# ClassBegin КлассСоСвойством;
Перем Свойство Экспорт;

Процедура Конструктор()
	Свойство = 1;
КонецПроцедуры	// Конструктор

//------------------------------------ ------------------------------------
// 	Работа с динамическими переменными
//------------------------------------ ------------------------------------
Функция ПриПолучении_Свойство(ЗначениеАтрибута)
	Возврат Свойство;
КонецФункции

Процедура ПриЗаписи_Свойство(НовоеЗначениеАтриб)
	Свойство = НовоеЗначениеАтриб;
КонецПроцедуры
       
Процедура ИзменитьЗначениеСвойства(НовоеЗначение) Экспорт
	Свойство = НовоеЗначение;
КонецПроцедуры	// ИзменитьЗначениеСвойства

//# ClassEnd КлассСоСвойством;


//# ClassBegin КлассСДинамическимСвойством;

// используем открытое свойство "ДинамическоеСвойство"

Перем НенужноеСвойство; // важен сам факт наличия поля
Перем СкрытоеСвойство;
Перем Свойство Экспорт;

Функция Конт(Конт) Возврат Конт; КонецФункции
Функция Сам() Возврат Конт(Контекст); КонецФункции

Процедура Конструктор()
	СкрытоеСвойство = 1;
	Свойство = 100;
	
	Сам().ДобавитьДинамическоеСвойство("ДинамическоеСвойство");
КонецПроцедуры	// Конструктор

//------------------------------------ ------------------------------------
// 	Работа с динамическими переменными
//------------------------------------ ------------------------------------
Функция _ПриЧтенииСвойства(стрИмяАтрибута)
	Если стрИмяАтрибута = "ДинамическоеСвойство" Тогда
		Возврат СкрытоеСвойство;
	КонецЕсли;

	Модуль = СоздатьОбъект("ВыполняемыйМодуль");
	Модуль.ВыброситьИскл(, "Не найдено свойство <"+стрИмяАтрибута+">");
КонецФункции

Процедура _ПриЗаписиСвойства(стрИмяАтрибута,  НовоеЗначениеАтриб)
	Если стрИмяАтрибута = "ДинамическоеСвойство" Тогда
		СкрытоеСвойство = НовоеЗначениеАтриб;
		Возврат;
	КонецЕсли;
	
	Модуль = СоздатьОбъект("ВыполняемыйМодуль");
	Модуль.ВыброситьИскл(, "Не найдено свойство <"+стрИмяАтрибута+">");
КонецПроцедуры
       
Процедура ИзменитьЗначениеСвойства(НовоеЗначение) Экспорт
	СкрытоеСвойство = НовоеЗначение;
КонецПроцедуры	// ИзменитьЗначениеСвойства

Функция ПолучитьСвойство() Экспорт
	Возврат СкрытоеСвойство;
КонецФункции
     
Процедура ДобавитьДинамическоеСвойство_Метод(Имя) Экспорт
	Сам().ДобавитьДинамическоеСвойство(Имя);
КонецПроцедуры

//# ClassEnd КлассСДинамическимСвойством;

//# ClassBegin КлассСДинамическимСвойством_Наследник;

// используем открытое свойство "ДинамическоеСвойствоНаследника"

Перем НенужноеСвойство; // важен сам факт наличия поля
Перем СкрытоеСвойство1;
//Перем Свойство Экспорт;

Функция Конт(Конт) Возврат Конт; КонецФункции
Функция Сам() Возврат Конт(Контекст); КонецФункции

Процедура Конструктор()
	СкрытоеСвойство1 = 1;
	Сам().Свойство = 200;
	
	Сам().ДобавитьДинамическоеСвойство("ДинамическоеСвойствоНаследника");
КонецПроцедуры	// Конструктор

//------------------------------------ ------------------------------------
// 	Работа с динамическими переменными
//------------------------------------ ------------------------------------
Функция _ПриЧтенииСвойства(стрИмяАтрибута)
	Если стрИмяАтрибута = "ДинамическоеСвойствоНаследника" Тогда
		Возврат СкрытоеСвойство1;
	КонецЕсли;

	Модуль = СоздатьОбъект("ВыполняемыйМодуль");
	Модуль.ВыброситьИскл(, "Не найдено свойство <"+стрИмяАтрибута+">");
КонецФункции

Процедура _ПриЗаписиСвойства(стрИмяАтрибута,  НовоеЗначениеАтриб)
	Если стрИмяАтрибута = "ДинамическоеСвойствоНаследника" Тогда
		СкрытоеСвойство1 = НовоеЗначениеАтриб;
		Возврат;
	КонецЕсли;
	
	Модуль = СоздатьОбъект("ВыполняемыйМодуль");
	Модуль.ВыброситьИскл(, "Не найдено свойство <"+стрИмяАтрибута+">");
КонецПроцедуры
       
Процедура ИзменитьЗначениеСвойстваНаследника(НовоеЗначение) Экспорт
	СкрытоеСвойство1 = НовоеЗначение;
КонецПроцедуры	// ИзменитьЗначениеСвойства
     
Процедура ДобавитьДинамическоеСвойство_Метод(Имя) Экспорт
	Сам().ДобавитьДинамическоеСвойство(Имя);
КонецПроцедуры

//# ClassEnd КлассСДинамическимСвойством_Наследник;