// работа с наследованием

//# ClassBegin ООППредокВОдномФайлеСОдинаковымиМетодами;

Перем АтрибутБазовогоКласса Экспорт;

Перем Журнал Экспорт;

Функция Сам(Конт)
	Возврат Конт;
КонецФункции

Функция ПолучитьЖурналВызовов() Экспорт
	//Журнал.ДобавитьЗапись("ООППредок::ПолучитьЖурналВызовов");
	Возврат Журнал;
КонецФункции

Процедура Конструктор()
	Сам(Контекст).АтрибутБазовогоКласса = 1;

	Журнал=СоздатьОбъект("ЖурналВызоваМетодов");
	Журнал.ДобавитьЗапись("ООППредокВОдномФайлеСОдинаковымиМетодами::Конструктор");
КонецПроцедуры	// Конструктор

Процедура Деструктор()                                   
	Журнал.ДобавитьЗапись("ООППредокВОдномФайлеСОдинаковымиМетодами::Деструктор");
КонецПроцедуры // Деструктор

//# ClassEnd ООППредокВОдномФайлеСОдинаковымиМетодами;

//# ClassBegin ООППотомокВОдномФайлеСОдинаковымиМетодами;

Функция Сам(Конт)
	Возврат Конт;
КонецФункции

Процедура Конструктор()
	Сам(Контекст).АтрибутБазовогоКласса = 2;

	Сам(Контекст).ПолучитьЖурналВызовов().ДобавитьЗапись("ООППотомокВОдномФайлеСОдинаковымиМетодами::Конструктор");
КонецПроцедуры	// Конструктор

Процедура Деструктор()                                   
	Сам(Контекст).ПолучитьЖурналВызовов().ДобавитьЗапись("ООППотомокВОдномФайлеСОдинаковымиМетодами::Деструктор");
КонецПроцедуры // Деструктор

//# ClassEnd ООППотомокВОдномФайлеСОдинаковымиМетодами;
