Перем миф;

Функция Конт(Конт) Возврат Конт; КонецФункции
Функция Сам() Возврат Конт(Контекст); КонецФункции
Функция База() Возврат Конт(Контекст).ПолучитьБазовыйКласс(); КонецФункции

Процедура Конструктор()                                   
	миф=СоздатьОбъект("MetaInfoClasses");
КонецПроцедуры

Функция ПараметрПустаяСтрока(_Парам) Экспорт
	Возврат _Парам;
КонецФункции

Функция ПараметрСтрока(_Парам) Экспорт
	Возврат _Парам;
КонецФункции

Процедура Тест_ПустаяСтрокаТип() Экспорт
	
	Объект = СоздатьОбъект("тестыЗначенияПараметровПоУмолчанию");
	
	Сам().ПроверитьРавенство(ТипЗначенияСтр(Объект.ПараметрПустаяСтрока()), "Строка");
	
КонецПроцедуры

Процедура Тест_ПустаяСтрокаЗначение() Экспорт
	
	Объект = СоздатьОбъект("тестыЗначенияПараметровПоУмолчанию");
	                       
	Сам().ПроверитьРавенство(Объект.ПараметрПустаяСтрока(), "");
	
КонецПроцедуры

Процедура Тест_СтрокаТип() Экспорт
	
	Объект = СоздатьОбъект("тестыЗначенияПараметровПоУмолчанию");
	
	Сам().ПроверитьРавенство(ТипЗначенияСтр(Объект.ПараметрСтрока()), "Строка");
	
КонецПроцедуры

Процедура Тест_СтрокаЗначение() Экспорт
	
	Объект = СоздатьОбъект("тестыЗначенияПараметровПоУмолчанию");
	                       
	Сам().ПроверитьРавенство(Объект.ПараметрСтрока(), "ПараметрСтрока");
	
КонецПроцедуры

Процедура Метод1_СПараметромПоУмолчанию(Список) Экспорт
	Сам().Метод2_СПараметромПоУмолчанию(Список);
КонецПроцедуры

Процедура Метод2_СПараметромПоУмолчанию(Список) Экспорт
КонецПроцедуры

Процедура Метод3_СПараметромПоУмолчанию(Объект) Экспорт
КонецПроцедуры

Процедура Метод4_СПараметромПоУмолчанию(Объект) Экспорт
КонецПроцедуры

Процедура Метод5_СПараметромПоУмолчанию(Объект) Экспорт
КонецПроцедуры

Процедура Метод6_СПараметромПоУмолчанию(Объект) Экспорт
КонецПроцедуры

Процедура Тест_ПроверкиТиповПараметров() Экспорт
	Сам=Сам();
	миф.УстановитьТекУстановки(, 1,);
	Сам.ПроверитьИсключение(Сам, "Метод2_СПараметромПоУмолчанию", "Строка");
	Сам.Метод3_СПараметромПоУмолчанию();
	Сам.ПроверитьИсключение(Сам, "Метод4_СПараметромПоУмолчанию");
	Сам.ПроверитьИсключение(Сам, "Метод5_СПараметромПоУмолчанию");
//!!!!
Сам.ПропуститьТест();
	Сам.ПроверитьИсключение(Сам, "Метод6_СПараметромПоУмолчанию");
КонецПроцедуры

Процедура Тест_ПередачаПараметраПоУмолчанию() Экспорт
	миф.УстановитьТекУстановки(, 1,);
	Сам().Метод1_СПараметромПоУмолчанию();
КонецПроцедуры

Процедура Метод1_СПараметромНеопределенногоТипа(Тип, Значение, Парам) Экспорт
	Тип = ТипЗначенияСтр(Парам);
	Значение = Парам;
КонецПроцедуры

Процедура Метод2_СПараметромНеопределенногоТипа(Тип, Значение, Парам) Экспорт
	Тип = ТипЗначенияСтр(Парам);
	Значение = Парам;
КонецПроцедуры

Процедура Тест_ПередачаПараметраПоУмолчанию_НеопределенныйТип1() Экспорт
Перем Тип, Значение;

	Сам=Сам();
	миф.УстановитьТекУстановки(, 0,);
	
	Сам.Метод1_СПараметромНеопределенногоТипа(Тип, Значение);
//!!!!
Сам.ПропуститьТест();
	Сам.ПроверитьРавенство(Тип, "Строка");
	Сам.ПроверитьРавенство(Значение, "");
КонецПроцедуры

Процедура Тест_ПередачаПараметраПоУмолчанию_НеопределенныйТип2() Экспорт
Перем Тип, Значение;
	
	Сам=Сам();
	миф.УстановитьТекУстановки(, 0,);
	Сам.Метод2_СПараметромНеопределенногоТипа(Тип, Значение);
//!!!!
Сам.ПропуститьТест();
	Сам.ПроверитьРавенство(Тип, "Строка");
	Сам.ПроверитьРавенство(Значение, "123");
КонецПроцедуры

Функция Метод_ПараметрыПоУмолчанию_Сложение(А = 2, Б = 3) Экспорт
	Возврат А+Б;
КонецФункции

Процедура Тест_ГлобальнаяФункция_ШтатныеПараметрыПоУмолчанию() Экспорт
	Сам=Сам();
//Таб = СоздатьОбъект("ТаблицаЗначений");
//Таб.ВыбратьСтроку();

	Значение = глШтатныеПараметрыПоУмолчанию_Сложение(4);
	Сам.ПроверитьРавенство(Значение, 7);

	Значение = глШтатныеПараметрыПоУмолчанию_Сложение(,4);
	Сам.ПроверитьРавенство(Значение, 6);

	Значение = глШтатныеПараметрыПоУмолчанию_Сложение(4,);
	
	Сам.ПропуститьТест(0, "Штатные параметры по умолчанию неверно работают !!"+РазделительСтрок+
		"       Например, глоб. функция глШтатныеПараметрыПоУмолчанию_Сложение(4,) должна вернуть 7, "+РазделительСтрок+
		"       но 1С возвращает 4 :("+РазделительСтрок);
	
	Сам.ПроверитьРавенство(Значение, 7); // БАГ 1С - система возвращает 4
КонецПроцедуры

Процедура Тест_МетодКласса_ПараметрыПоУмолчанию() Экспорт
	Сам=Сам();

	Значение = Сам.Метод_ПараметрыПоУмолчанию_Сложение(4);
	Сам.ПроверитьРавенство(Значение, 7);

	Значение = Сам.Метод_ПараметрыПоУмолчанию_Сложение(,4);
	Сам.ПроверитьРавенство(Значение, 6);

	Значение = Сам.Метод_ПараметрыПоУмолчанию_Сложение(4,);
	Сам.ПроверитьРавенство(Значение, 7);
КонецПроцедуры

Функция Метод_ШтатныеПараметрыПоУмолчанию_Сложение_3Параметра(А = 2, Б = 3, Ц = 5) Экспорт
	Возврат А + Б + Ц;
КонецФункции

Процедура Тест_ГлобальнаяФункция_ШтатныеПараметрыПоУмолчанию_3Параметра() Экспорт
	Сам=Сам();

	Сам.ПропуститьТест(0, "Штатные параметры по умолчанию неверно работают !!"+РазделительСтрок+
		"       Например, глоб. функция глШтатныеПараметрыПоУмолчанию_Сложение(4,) должна вернуть 7, "+РазделительСтрок+
		"       но 1С возвращает 4 :("+РазделительСтрок);

	Значение = глШтатныеПараметрыПоУмолчанию_Сложение_3Параметра(4);
	Сам.ПроверитьРавенство(Значение, 12);

	Значение = глШтатныеПараметрыПоУмолчанию_Сложение_3Параметра(,4);
	Сам.ПроверитьРавенство(Значение, 11);

	Значение = глШтатныеПараметрыПоУмолчанию_Сложение_3Параметра(4,);
	Сам.ПроверитьРавенство(Значение, 12);

	Значение = глШтатныеПараметрыПоУмолчанию_Сложение_3Параметра(4, 1);
	Сам.ПроверитьРавенство(Значение, 10);

	Значение = глШтатныеПараметрыПоУмолчанию_Сложение_3Параметра(4, 1, );
	Сам.ПроверитьРавенство(Значение, 10);

	Значение = глШтатныеПараметрыПоУмолчанию_Сложение_3Параметра(, 1);
	Сам.ПроверитьРавенство(Значение, 8);

	Значение = глШтатныеПараметрыПоУмолчанию_Сложение_3Параметра(, 1, );
	Сам.ПроверитьРавенство(Значение, 8);
КонецПроцедуры

Процедура Тест_МетодКласса_ПараметрыПоУмолчанию_3Параметра() Экспорт
	Сам=Сам();

	Значение = Сам.Метод_ШтатныеПараметрыПоУмолчанию_Сложение_3Параметра(4);
	Сам.ПроверитьРавенство(Значение, 12);

	Значение = Сам.Метод_ШтатныеПараметрыПоУмолчанию_Сложение_3Параметра(,4);
	Сам.ПроверитьРавенство(Значение, 11);

	Значение = Сам.Метод_ШтатныеПараметрыПоУмолчанию_Сложение_3Параметра(4,);
	Сам.ПроверитьРавенство(Значение, 12);

	Значение = Сам.Метод_ШтатныеПараметрыПоУмолчанию_Сложение_3Параметра(4, 1);
	Сам.ПроверитьРавенство(Значение, 10);

	Значение = Сам.Метод_ШтатныеПараметрыПоУмолчанию_Сложение_3Параметра(4, 1, );
	Сам.ПроверитьРавенство(Значение, 10);

	Значение = Сам.Метод_ШтатныеПараметрыПоУмолчанию_Сложение_3Параметра(, 1);
	Сам.ПроверитьРавенство(Значение, 8);

	Значение = Сам.Метод_ШтатныеПараметрыПоУмолчанию_Сложение_3Параметра(, 1, );
	Сам.ПроверитьРавенство(Значение, 8);
КонецПроцедуры

Функция Метод_СПараметромПоУмолчаниюОпределенныйТолькоВПрмФайле(Парам) Экспорт
	Возврат Парам;
КонецФункции

Функция Метод_СПараметромПоУмолчаниюОпределенныйВПрмФайлеИВМодулеКласса(Парам = "ЗначениеМодульКласса") Экспорт
	Возврат Парам;
КонецФункции

Функция Метод_СПараметромПоУмолчаниюОпределенныйВМодулеКлассаИОписанныйВПрмФайле(Парам = "ЗначениеМодульКласса") Экспорт
	Возврат Парам;
КонецФункции

//этот метод только в модуле класса 
Функция Метод_СПараметромПоУмолчаниюОпределенныйТолькоВМодулеКласса(Парам = "ЗначениеМодульКласса") Экспорт
	Возврат Парам;
КонецФункции

Процедура Проверить_Метод_СПараметромПоУмолчаниюОпределенныйВПрмФайлеИВМодулеКласса(Объект)
	Сам=Сам();
                                       
	Значение = Объект.Метод_СПараметромПоУмолчаниюОпределенныйТолькоВПрмФайле();
	Сам.ПроверитьРавенство(Значение, "ЗначениеПрм");

	Значение = Объект.Метод_СПараметромПоУмолчаниюОпределенныйВПрмФайлеИВМодулеКласса();
	Сам.ПроверитьРавенство(Значение, "ЗначениеПрм");

	Сам.ПроверитьИсключение(Объект, "Метод_СПараметромПоУмолчаниюОпределенныйВМодулеКлассаИОписанныйВПрмФайле");

	Значение = Объект.Метод_СПараметромПоУмолчаниюОпределенныйТолькоВМодулеКласса();
	Сам.ПроверитьРавенство(Значение, "ЗначениеМодульКласса");
КонецПроцедуры

Процедура Тест_Метод_СПараметромПоУмолчаниюОпределенныйВПрмФайлеИВМодулеКласса() Экспорт
	Объект = Сам();
	Проверить_Метод_СПараметромПоУмолчаниюОпределенныйВПрмФайлеИВМодулеКласса(Объект);
КонецПроцедуры

Процедура Тест_ДляНаследника_Метод_СПараметромПоУмолчаниюОпределенныйВПрмФайлеИВМодулеКласса() Экспорт
	Объект = СоздатьОбъект("Наследник_ТестыЗначенияПараметровПоУмолчанию");
	Проверить_Метод_СПараметромПоУмолчаниюОпределенныйВПрмФайлеИВМодулеКласса(Объект);
КонецПроцедуры
