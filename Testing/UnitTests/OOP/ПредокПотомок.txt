// работа с наследованием

//# ClassBegin ООППредокВОдномФайле;
Перем АтрибутБазовогоКласса Экспорт;

// проверка бага 1821 http://cvs.alterplast.ru/bugs/show_bug.cgi?id=1821
// если написать без "Экспорт", наследования не будет
Функция _ПолучитьКод()
	Возврат "ОбъектООППредокВОдномФайле";
КонецФункции	//

Функция СоздатьОбъект_(ТипОбъекта) Экспорт
	Возврат СоздатьОбъект(ТипОбъекта);
КонецФункции

//# ClassEnd ООППредокВОдномФайле;

//# ClassBegin ООППотомокВОдномФайле;

Функция Сам(Конт)
	Возврат Конт;
КонецФункции

Процедура Конструктор()
	Сам(Контекст).АтрибутБазовогоКласса = 2;
КонецПроцедуры	// Конструктор

//# ClassEnd ООППотомокВОдномФайле;

//# ClassBegin ПотомокНесуществующегоКласса;

//# ClassEnd ПотомокНесуществующегоКласса;

//# ClassBegin ПотомокОтВстроенногоКлассаСОграничениемДоступаКСвойству;

Функция Этот(Конт) Возврат Конт; КонецФункции
Функция Сам() Возврат Этот(Контекст); КонецФункции
Функция База() Экспорт Возврат Сам().ПолучитьБазовыйКласс(""); КонецФункции

//# ClassEnd ПотомокОтВстроенногоКлассаСОграничениемДоступаКСвойству;

//# ClassBegin СериализуемыйКласс;

Перем Атрибут1 Экспорт;
Перем Атрибут2 Экспорт;

Функция Этот(Конт) Возврат Конт; КонецФункции
Функция Сам() Возврат Этот(Контекст); КонецФункции

Процедура Конструктор()
	Атрибут1 = 1;
	Атрибут2 = 2;
КонецПроцедуры

Процедура ИзменитьАтрибуты() Экспорт
	Атрибут1 = Атрибут1 + 1;
	Атрибут2 = Атрибут2 + 1;
КонецПроцедуры

Функция КлассСохраняемый()
	Возврат 1;
КонецФункции

Функция СохранитьКлассВСтроку()
	лСписокЗначений = СоздатьОбъект("СписокЗначений");
	лСписокЗначений.ДобавитьЗначение(Атрибут1);
	лСписокЗначений.ДобавитьЗначение(Атрибут2);

	стрРезультат = ЗначениеВСтрокуВнутр(лСписокЗначений);

	Возврат стрРезультат;
КонецФункции

Процедура ЗагрузитьИзСтроки(СериализованноеПредставление)

	лСписокЗначений = ЗначениеИзСтрокиВнутр(СериализованноеПредставление);
	Атрибут1 = лСписокЗначений.ПолучитьЗначение(1);
	Атрибут2 = лСписокЗначений.ПолучитьЗначение(2);
КонецПроцедуры

//# ClassEnd СериализуемыйКласс;

//# ClassBegin СериализуемыйКлассСоСвойством;
Перем Журнал Экспорт;

Перем Атрибут1 Экспорт;
Перем Атрибут2 Экспорт;

Функция Этот(Конт) Возврат Конт; КонецФункции
Функция Сам() Возврат Этот(Контекст); КонецФункции

Процедура Конструктор()
	Журнал = СоздатьОбъект("ЖурналВызоваМетодов");
	Журнал.ДобавитьЗапись("Конструктор");

	Атрибут1 = 1;
	Атрибут2 = 2;
КонецПроцедуры

Процедура ИзменитьАтрибуты() Экспорт
	Атрибут1 = Атрибут1 + 1;
	Атрибут2 = Атрибут2 + 1;
КонецПроцедуры

Функция КлассСохраняемый()
	Журнал.ДобавитьЗапись("КлассСохраняемый");
	Возврат 1;
КонецФункции

Функция СохранитьКлассВСтроку()
	Журнал.ДобавитьЗапись("СохранитьКлассВСтроку");

	лСписокЗначений = СоздатьОбъект("СписокЗначений");
	лСписокЗначений.ДобавитьЗначение(Атрибут1);
	лСписокЗначений.ДобавитьЗначение(Атрибут2);

	стрРезультат = ЗначениеВСтрокуВнутр(лСписокЗначений);

	Возврат стрРезультат;
КонецФункции

Процедура ЗагрузитьИзСтроки(СериализованноеПредставление)
	Журнал = СоздатьОбъект("ЖурналВызоваМетодов");
	Журнал.ДобавитьЗапись("ЗагрузитьИзСтроки");

	лСписокЗначений = ЗначениеИзСтрокиВнутр(СериализованноеПредставление);
	Атрибут1 = лСписокЗначений.ПолучитьЗначение(1);
	Атрибут2 = лСписокЗначений.ПолучитьЗначение(2);
КонецПроцедуры

//# ClassEnd СериализуемыйКлассСоСвойством;

//# ClassBegin СериализуемыйКлассСИсключениемВМетодеЗагрузитьИзСтроки;

Функция Этот(Конт) Возврат Конт; КонецФункции
Функция Сам() Возврат Этот(Контекст); КонецФункции

Процедура Конструктор()
КонецПроцедуры

Функция КлассСохраняемый()
	Возврат 1;
КонецФункции

Функция СохранитьКлассВСтроку()
	Возврат "";
КонецФункции

Процедура ЗагрузитьИзСтроки(СериализованноеПредставление)
	//Сам().ВыброситьИскл("Проверочное исключение", "Проверочное исключение");
	Модуль = СоздатьОбъект("ВыполняемыйМодуль");
	Модуль.ВыброситьИскл(, "Проверочное исключение");
КонецПроцедуры

//# ClassEnd СериализуемыйКлассСИсключениемВМетодеЗагрузитьИзСтроки;

//# ClassBegin СериализуемыйКлассСИсключениемВМетодеКлассСохраняемый;

Функция Этот(Конт) Возврат Конт; КонецФункции
Функция Сам() Возврат Этот(Контекст); КонецФункции

Процедура Конструктор()
КонецПроцедуры

Функция КлассСохраняемый()
	Модуль = СоздатьОбъект("ВыполняемыйМодуль");
	Модуль.ВыброситьИскл(, "Проверочное исключение");
	Возврат 1;
КонецФункции

Функция СохранитьКлассВСтроку()
	Возврат "";
КонецФункции

Процедура ЗагрузитьИзСтроки(СериализованноеПредставление)
КонецПроцедуры

//# ClassEnd СериализуемыйКлассСИсключениемВМетодеКлассСохраняемый;

//# ClassBegin СериализуемыйКлассСИсключениемВМетодеСохранитьКлассВСтроку;

Функция Этот(Конт) Возврат Конт; КонецФункции
Функция Сам() Возврат Этот(Контекст); КонецФункции

Процедура Конструктор()
КонецПроцедуры

Функция КлассСохраняемый()
	Возврат 1;
КонецФункции

Функция СохранитьКлассВСтроку()
	Модуль = СоздатьОбъект("ВыполняемыйМодуль");
	Модуль.ВыброситьИскл(, "Проверочное исключение");
	Возврат "";
КонецФункции

Процедура ЗагрузитьИзСтроки(СериализованноеПредставление)
КонецПроцедуры

//# ClassEnd СериализуемыйКлассСИсключениемВМетодеСохранитьКлассВСтроку;

//# ClassBegin КлассСФункциейВирт;
Перем гАтрибут Экспорт;

Функция Этот(Конт) Возврат Конт; КонецФункции

Процедура Конструктор()                                   
	гАтрибут = 1;
КонецПроцедуры // Конструктор

// все функции класса являются виртуальными и используются в вызовах через Наследника 

Функция виртПолучитьКонтекст() Экспорт
	Возврат Вирт();
КонецФункции

Функция виртПолучитьСебя() Экспорт
	Возврат я();
КонецФункции

Функция виртПолучитьАтрибутЧерезВирт() Экспорт
	Возврат вирт().гАтрибут;
КонецФункции

Функция виртПолучитьАтрибутЧерезЯ() Экспорт
	Возврат я().гАтрибут;
КонецФункции

//# ClassEnd КлассСФункциейВирт;

//# ClassBegin КлассСФункциейВиртНаследник;

Перем гАтрибут Экспорт;

Процедура Конструктор()                                   
	гАтрибут = 2;
КонецПроцедуры // Конструктор

Функция ПолучитьКонтекст_Наследник() Экспорт
	Возврат вирт();
КонецФункции

Функция ПолучитьСебя_Наследник() Экспорт
	Возврат я();
КонецФункции

Функция ПолучитьАтрибутЧерезВирт_Наследник() Экспорт
	Возврат вирт().гАтрибут;
КонецФункции

Функция ПолучитьАтрибутЧерезЯ_Наследник() Экспорт
	Возврат я().гАтрибут;
КонецФункции

//# ClassEnd КлассСФункциейВиртНаследник;
