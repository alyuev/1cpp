Функция Конт(Конт) Возврат Конт; КонецФункции
Функция Сам() Возврат Конт(Контекст); КонецФункции
Функция База(ИмяКласса) Возврат Конт(Контекст).ПолучитьБазовыйКласс(ИмяКласса); КонецФункции

// виртуальный метод для переопределения в классах-наследниках
// вызывается всегда после тестирования теста
//	неважно, удачно или неудачно завершился тест
// в нем не должно быть исключений (иначе вся система тестирования не будет работать)
//	ВАЖНО: при переопределении метода в классе наследнике
// НЕ забывать вызывать метод из базового класса
//

// После разделения родителей метод можно будет выбросить

Процедура Сброс() Экспорт
	База("ДелегатФормы").ЗакрытьФорму();
	База("ТестовыйСлучайСДанными").Сброс(); // обязательно !!
КонецПроцедуры
//--------------------------------------------
