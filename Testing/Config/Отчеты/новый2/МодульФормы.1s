
Перем 
		Стр Экспорт,
		Тест; 

Процедура МетодФормы1() Экспорт
	Сообщить("Вызов Док.МетодФормы1");
КонецПроцедуры
      
Процедура МетодФормы() Экспорт
	Сообщить("Вызов Док.МетодФормы");
КонецПроцедуры
      
// для проверки http://itland.ru/forum/index.php?showtopic=15237&st=40
Процедура МетодФормыСИключением() Экспорт
	ОБ = СоздатьОбъект("ВыполняемыйМодуль");
	ОБ.ВыброситьИскл("ОбъектИсключение", "СтрокаИсключение");
КонецПроцедуры	// МетодФормыСИключением

// проверка бага 2402 http://cvs.alterplast.ru/bugs/show_bug.cgi?id=2402
 
Функция ВзятьКонтекст(Конт)
	Возврат Конт;
КонецФункции	// ВзятьКонтекст
          
Процедура Выполнить()
	Сообщить("Выполнить");
	Конт = ВзятьКонтекст(Контекст);
	
	////Конт.МетодФормы();
	//Конт.МетодФормы1();
	//Конт.МетодФормы();
	МетодФормы1();
	//а = 1/0; 
	//Модуль = СоздатьОбъект("ВыполняемыйМодуль");
	//Модуль.ВыброситьИскл(, "dfgdfg");
КонецПроцедуры	// Выполнить

//Процедура ПослеОткрытия()
//	Сообщить("Документ ПослеОткрытия = ");
//	//ВМ = СоздатьОбъект("ВыполняемыйМодуль");
//	//ВМ.GetMethOfContext(Контекст);
//
//Тест = СоздатьОбъект("НаследникГрупповойКонтекст");
////Тест.Инициализация(Контекст);
//Сообщить("======================================================= = ");	
//	Тест.ПерехватитьМетодыМодуляГК(Контекст);
//Сообщить("======================================================= = ");	
//	//Тест.ПослеОткрытия();
//
//	//Выполнить();
////	Сообщить("Документ ПослеОткрытия 2");
//
//КонецПроцедуры

//============================================================
Процедура ПриОткрытии()
Тест = СоздатьОбъект("НаследникГрупповойКонтекст");
Тест.Инициализация(Контекст);
Сообщить("======================================================= = ");	
	Тест.ПерехватитьСобытияГК(Контекст);
Сообщить("======================================================= = ");	
КонецПроцедуры

//============================================================
Процедура ПриНачалеВыбораЗначения(ИдентификаторЭлемента, Флаг)
	Сообщить("ПриНачалеВыбораЗначения");
	Сообщить("СтатусВозврата(0);");
	
	СтатусВозврата(0);
КонецПроцедуры

//*******************************************
Процедура Сформировать()
	//Предупреждение("Отчет.новый2  Сформировать()");
КонецПроцедуры
